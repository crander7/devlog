[tools]
# Primary runtime for app, CLI, and scripts
bun = "1.3.9"
# Used by build:routes (npx @tanstack/router-cli) and typecheck (tsc)
node = "24.10.0"
# Lint/format – matches devDependency; optional for running biome without installing deps
# biome = "2.3.8"
